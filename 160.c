#include<stdio.h>
#include <string.h>
int main(){
    char s1[26],s2[26];
    gets(s1);gets(s2);
    int len1=strlen(s1),len2=strlen(s2);
    for(int i=0;i<len1;i++){
        int flag=0;
        char a;
    }

    return 0;
}